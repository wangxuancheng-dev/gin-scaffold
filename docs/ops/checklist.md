# 上线检查清单（Ops 入口）

为避免与根目录清单重复维护，**权威内容已合并至一份**：

👉 **[完整上线检查清单](/checklist)**（`docs/checklist.md`）

该页包含：构建与 `golangci-lint`、配置与密钥、API + **Worker** 进程、网关与 `/metrics`/`/swagger`、上线巡检、**handler 错误返回规范**、回滚与 Hotfix。

从运维文档侧边栏进入本页时，请直接打开上方链接完成打勾。
